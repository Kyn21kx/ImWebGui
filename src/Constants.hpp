#pragma once

class Constants {
	public:
	// this improves the quality of the font at the expense of a memory and load time
	static constexpr float kFontScale = 3.00f;

	static constexpr float HEADER_HEIGHT = 80.0f;
};
